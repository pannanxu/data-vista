"use client"
import React from "react";
import {LineProps} from "./types";

const Line = (props: LineProps) => {
    console.log(props)
    return (
        <div>
            <h1>line</h1>
        </div>
    );
};

export default Line