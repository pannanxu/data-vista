import * as React from "react";
import {LineProps} from "./types";

const Line: React.FC<LineProps> = () => {
    return (
        <div>
            <h1>line</h1>
        </div>
    );
};

export default Line